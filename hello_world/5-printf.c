
#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Print the string "with proper grammar, but the outcome is a
 * piece of art," and exit.
 * Return: Always 0 (Succees)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}

