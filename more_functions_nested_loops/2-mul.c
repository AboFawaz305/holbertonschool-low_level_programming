/**
 * mul - mutliply a and b
 * @a: the first multiplication operand
 * @a: the second multiplication operand
 *
 * Return: the result of multiplying a with b
 */
int mul(int a, int b)
{
	return (a*b);
}
